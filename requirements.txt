# R2AI GPU/SSH runtime requirements.
#
# Target environment:
# - Linux GPU server
# - Python 3.10 or 3.11
# - NVIDIA driver new enough for CUDA 12.1 wheels
#
# If torch installation fails on your rented GPU image, install PyTorch from:
# https://pytorch.org/get-started/locally/
# then rerun: python -m pip install -r requirements.txt --no-deps

--extra-index-url https://download.pytorch.org/whl/cu121

numpy>=1.26,<3
torch>=2.4.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.4

sentence-transformers>=3.0.1
huggingface-hub>=0.24.6
datasets>=2.20.0
pyarrow>=16.1.0

faiss-cpu>=1.8.0
scikit-learn>=1.5.0
scipy>=1.11.0
pandas>=2.2.0
tqdm>=4.66.0

sentencepiece>=0.2.0
protobuf>=4.25.3
tokenizers>=0.19.1
einops>=0.8.0
hf-transfer>=0.1.8
